import {{camelCase name}} from '@/components/{{pascalCase name}}/{{camelCase name}}.module.scss';

interface {{pascalCase name}}Props {
  // Add your component props here
}

const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = (props) => {
  return (
    <div className={ {{camelCase name}}.container } data-testid="{{camelCase name}}">
      {{pascalCase name}} Component
    </div>
  );
}

export default {{pascalCase name}};
